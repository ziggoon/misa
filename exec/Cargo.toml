[package]
name = "exec"
version = "0.1.0"
edition = "2021"

# See more keys and their definitions at https://doc.rust-lang.org/cargo/reference/manifest.html

[dependencies]
libc = "0.2.153"
open = { path = "../open" }
config = { path = "../config" }
unlink = { path = "../unlink" }
errno = "0.3.8"